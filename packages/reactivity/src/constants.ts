export enum ReactiveFlags {
  IS_REACTIVE = '__v_isReactive',
  IS_REF = '__v_isRef',
}

// 对象trigger类型
export enum TriggerOpTypes {
  SET = 'set',
  ADD = 'add',
  DELETE = 'delete',
  CLEAR = 'clear',
}
